import React from 'react';

import './ItemCards.css';

import Card from './Card/Card';

const itemCards = (props) => (
    <div className='ItemCards'>
        <Card image={} title={} startingPrice={} endingPrice={} />
    </div>
);

export default itemCards;